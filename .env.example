# Phoenix Server Configuration
PHX_SERVER=true
PHX_HOST=localhost
PORT=4000

# Security
SECRET_KEY_BASE=your-secret-key-base-generate-with-mix-phx-gen-secret

# Environment
MIX_ENV=prod

# DNS Cluster (optional, for distributed deployments)
DNS_CLUSTER_QUERY=

# SSL Configuration (optional)
# SOME_APP_SSL_KEY_PATH=/path/to/ssl/key.pem
# SOME_APP_SSL_CERT_PATH=/path/to/ssl/cert.pem

# Email Configuration (if using Swoosh)
# MAILGUN_API_KEY=your-mailgun-api-key
# MAILGUN_DOMAIN=your-mailgun-domain

# Database Configuration (if you add a database later)
# DATABASE_URL=postgres://username:password@hostname:5432/database_name

# Logging Level
# LOG_LEVEL=info

# Health Check Configuration
HEALTH_CHECK_PATH=/health
